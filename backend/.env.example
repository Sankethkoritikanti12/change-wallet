# Copy this to .env and fill in your values
# Never commit .env to git

DATABASE_URL=postgresql://your_user:your_password@localhost:5432/change_wallet
PORT=4000

# The register uses this key in the Authorization header
# Must match what's stored in the stores.register_api_key column
REGISTER_API_KEY=test-register-key-12345
